---Comandos do terminal: ---
pip install flask
pip install Flask mysql-connector-python

---Comandos do prompt: ---
git init
git config --global user.email "****@gmail.com"
git config --global user.name "mb7-alt"
git status
git add .
git remote add origin https://github.com/mb7-alt/mm.git
git push
